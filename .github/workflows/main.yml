name: testing the code

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - name: create file
        run: |
          mkdir app
          echo "Hello ashish" >> app/index.html
          cat app/index.html